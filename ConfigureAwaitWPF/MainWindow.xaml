<Window x:Class="ConfigureAwaitWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConfigureAwaitWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0"
                 Margin="20"
                 x:Name="MyCrashLabel">
            
        </TextBox>

        <Button Click="CrashButton_Click"
                Content="Do crash Work!"
                Grid.Row="1"
                Margin="25"/>

        <Button Command="{Binding BadCommand}"
                Content="Do bad Work!"
                Grid.Row="2"
                Margin="25"/>

        <Button Command="{Binding NiceCommand}"
                Content="Do nice Work!"
                Margin="25"
                Grid.Row="3"/>

        <Button Command="{Binding DeadlockCommand}"
                Content="Do deadlock Work!"
                Margin="25"
                Grid.Row="4"/>
    </Grid>
</Window>
